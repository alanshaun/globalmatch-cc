/**
 * [V2.0 DEPRECATED] Bing Search API Scraper
 *
 * 已废弃原因:
 *   - 每月仅 1000 次免费请求，超出即失败
 *   - 需要 BING_API_KEY（未配置时静默返回空）
 *   - 已由 SearXNG 替代（src/scrapers/searxng.ts）
 *
 * 此文件保留仅作历史参考，不再被任何代码导入。
 */

export {};
