/**
 * [V2.0 DEPRECATED] DuckDuckGo HTML Scraper
 *
 * 已废弃原因:
 *   - 依赖 DDG HTML 结构正则解析，随时失效
 *   - 每条 query 间 800-1500ms 强制延迟，性能差
 *   - 已由 SearXNG 替代（src/scrapers/searxng.ts）
 *
 * 此文件保留仅作历史参考，不再被任何代码导入。
 */

export {};
